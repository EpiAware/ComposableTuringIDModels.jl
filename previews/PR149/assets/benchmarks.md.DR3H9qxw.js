import{_ as a,o as s,c as t,ao as n}from"./chunks/framework.US6ydpSk.js";const m=JSON.parse('{"title":"Benchmarks","description":"","frontmatter":{},"headers":[],"relativePath":"benchmarks.md","filePath":"benchmarks.md","lastUpdated":null}'),o={name:"benchmarks.md"};function i(r,e,d,c,l,h){return s(),t("div",null,[...e[0]||(e[0]=[n('<h1 id="benchmarks" tabindex="-1">Benchmarks <a class="header-anchor" href="#benchmarks" aria-label="Permalink to &quot;Benchmarks {#benchmarks}&quot;">​</a></h1><p><code>ComposableTuringIDModels</code> tracks the performance of representative modelling operations over time. The suite is a prototype: it covers a small set of representative models rather than exhaustively measuring every component, and the numbers are indicative rather than a guarantee.</p><p>Benchmarking reuses the shared tooling in <code>EpiAwarePackageTools.Benchmarks</code> rather than re-implementing a runner or a comparison report. The package owns the suite definition (<code>benchmark/benchmarks.jl</code>); the kit owns running it and turning results into a legible pull-request comment.</p><h2 id="What-is-measured" tabindex="-1">What is measured <a class="header-anchor" href="#What-is-measured" aria-label="Permalink to &quot;What is measured {#What-is-measured}&quot;">​</a></h2><p>The suite is a <code>BenchmarkTools.BenchmarkGroup</code> named <code>SUITE</code>, defined in <code>benchmark/benchmarks.jl</code>, with three groups.</p><ul><li><p><strong>Model evaluation</strong> — building and evaluating representative models. For each model the suite times a prior draw (<code>rand</code>, which samples every random variable) and the forward pass (<code>model()</code>, which returns the generated quantities). The models are two latent processes (<code>AR</code>, <code>RandomWalk</code>) and two composed <code>IDModel</code>s (<code>DirectInfections</code> with <code>PoissonError</code>, and <code>Renewal</code> with <code>NegativeBinomialError</code>), each turned into a Turing model via <code>as_turing_model</code>.</p></li><li><p><strong>Sampling</strong> — a short NUTS run (50 draws) on a composed <code>DirectInfections</code> + <code>PoissonError</code> model conditioned on data simulated from its own prior.</p></li><li><p><strong>AD gradients</strong> — the gradient of a representative log-density across automatic-differentiation backends (<code>ForwardDiff</code>, <code>ReverseDiff</code>, <code>Mooncake</code>, and <code>Enzyme</code> where supported). Results are keyed by scenario and backend so the comparison report folds them into a per-(scenario × backend) matrix.</p></li></ul><h2 id="Running-the-suite-locally" tabindex="-1">Running the suite locally <a class="header-anchor" href="#Running-the-suite-locally" aria-label="Permalink to &quot;Running the suite locally {#Running-the-suite-locally}&quot;">​</a></h2><p>The <code>benchmark/</code> directory is its own Julia environment. Run the whole suite and save the results with the managed runner, which calls <code>EpiAwarePackageTools.Benchmarks.run_suite</code>:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=benchmark</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> benchmark/run.jl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> results.json</span></span></code></pre></div><p><code>run_suite</code> uses a short per-benchmark time budget so a full run stays affordable while the minimum-time estimator used in the comparison stays stable. To compare two result files and write a Markdown report, use the managed comparison script, which calls <code>EpiAwarePackageTools.Benchmarks.compare_comment</code>:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=benchmark</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> benchmark/compare.jl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pr.json</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> base.json</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> comment.md</span></span></code></pre></div><h2 id="Continuous-integration" tabindex="-1">Continuous integration <a class="header-anchor" href="#Continuous-integration" aria-label="Permalink to &quot;Continuous integration {#Continuous-integration}&quot;">​</a></h2><p>Two workflows drive benchmarking in CI, both building on the shared kit.</p><ul><li><p><code>benchmark.yaml</code> runs on pull requests. It benchmarks the pull-request head and the base branch in separate jobs, then posts (and updates) a single comparison comment: a bucketed summary plus collapsed per-benchmark tables split into evaluation and AD-gradient groups.</p></li><li><p><code>benchmark-history.yaml</code> runs on pushes to <code>main</code> and on tags. It benchmarks the recent tagged releases plus the current commit with AirspeedVelocity and publishes a timeline to the repository&#39;s <code>benchmarks</code> branch. The kit&#39;s <code>asv_comment</code> / <code>flatten_asv</code> helpers read the same AirspeedVelocity result format when a report is needed.</p></li></ul>',14)])])}const u=a(o,[["render",i]]);export{m as __pageData,u as default};
